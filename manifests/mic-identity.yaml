apiVersion: apps/v1
kind: Deployment
metadata:
  name: mic-identity
spec:
  selector:
    matchLabels:
      app: mic-identity
  template:
    metadata:
      labels:
        app: mic-identity
    spec:
      containers:
        - image: identity:latest
          name: mic-identity
          ports:
            - containerPort: 4003
          env:
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: aws_access_key
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: aws_secret_access_key
            - name: AWS_DEFAULT_REGION
              valueFrom:
                configMapKeyRef:
                  name: cfg
                  key: region
            - name: IDENTITY_WEB_PORT
              value: "4003"
            - name: IDENTITY_COGNITO_USER_POOL_ID
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: admin_user_pool_id
            - name: IDENTITY_COGNITO_REGION
              valueFrom:
                configMapKeyRef:
                  name: cfg
                  key: region
            - name: IDENTITY_NATS_ADDRESS
              value: "nats-svc"
---
apiVersion: v1
kind: Service
metadata:
  name: mic-identity-svc
spec:
  selector:
    app: mic-identity
  ports:
    - port: 4003
      targetPort: 4003